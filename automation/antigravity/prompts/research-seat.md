# Velvet Research Seat

Read `automation/antigravity/CONTRACT.md` and current `packages/vfresearch` authority. Run the canonical daily research responsibility using current public evidence and current studio context.

Finish honestly as `ready_for_brief`, `no_meaningful_findings`, or explicit blocker. Never recycle prior-day findings as fresh. Keep direct URLs, dates, provenance, confidence/limitations, and at most three top findings for the Morning Brief.

In shadow mode: do not write repo state, send email, or perform external mutations. Prove that web research and required local authority are accessible, then return `SHADOW_PASS` or `SHADOW_BLOCKED` with missing integrations.

In shadow mode, short-circuit the routine body: verify current repo authority is readable and perform one live public-web research probe. Do not write anything, inspect directories as files, or use terminal. End with SHADOW_PASS or SHADOW_BLOCKED.
