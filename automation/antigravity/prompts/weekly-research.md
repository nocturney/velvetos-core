# Weekly Research Accountability

Read `automation/antigravity/CONTRACT.md` and current weekly research/accountability authority. Review Sunday through Friday research/recommendations, verified implementation evidence, experiments, blockers and carryovers. Discussion or PR creation alone is not implementation.

Production output uses the CURRENT V10.3 rich RTL owner-email quality, exact owner Visible Text Gate and only the canonical GitHub/Gmail OAuth sender with real message ID and safe reset.

In shadow mode: do not email or mutate trackers. Verify that the same-week research history, implementation evidence and required live sources are accessible; return `SHADOW_PASS` or `SHADOW_BLOCKED`.

When VELVETOS_LIVE_CONTEXT_PATH is present, use it as fresh provider evidence for current business context; do not require the Google Workspace preview MCP.

In shadow mode, short-circuit the routine body. Verify VELVETOS_LIVE_CONTEXT_PATH, weekly research authority/history and owner-email V10.3/canonical sender files are readable. Do not email, mutate trackers, inspect directories as files, or use terminal. End with SHADOW_PASS or SHADOW_BLOCKED.
