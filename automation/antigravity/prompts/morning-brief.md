# Velvet Morning Brief

Read `automation/antigravity/CONTRACT.md` and current Morning Brief/office authority. Build the canonical 09:00 owner-control-plane decision from current live business sources, same-day Research Seat state, current Instagram evidence and current system state.

The production result must use full CURRENT V10.3 rich RTL rendering, exact owner Visible Text Gate, canonical GitHub/Gmail OAuth sender, real Gmail message ID and safe request reset. If same-day research is `no_meaningful_findings`, use the canonical honest empty state rather than stale findings.

In shadow mode: do not render/send an owner email and do not mutate any source. Verify that every required live source and sender prerequisite is actually reachable from Antigravity. Return `SHADOW_PASS` only if equivalent production execution is possible; otherwise `SHADOW_BLOCKED` naming each missing capability.
