# Velvet Factory — Brand Asset + Public CTA Lock

Status: **MANDATORY · FAIL-CLOSED**

This lock applies to every public Velvet Factory image, carousel, Story still, Reel cover, feed mockup, ad-like creative and caption.

## Logo / wordmark law

Never invent, redraw, approximate, stylize or ask a generative model to create the Velvet Factory logo, wordmark, emblem or a logo-like `VELVET FACTORY` lockup.

A logo/wordmark may appear only when an exact owner-approved brand asset is available to the current job and is cited by a concrete source reference. Apply it as a deterministic overlay after the image edit/generation step; do not bake it into generative imagery.

If no verified logo asset is available: **use no logo**. Do not create a substitute mark. A plain business-name text line is not permission to manufacture a logo-like treatment.
## Generative base-frame rule

For generative/editing prompts, explicitly require: **NO LOGO · NO WORDMARK · NO PHONE NUMBER · NO WHATSAPP · NO CONTACT BAR** unless an owner-approved exact logo asset is being composited deterministically afterward.

The visual model may edit background, light, crop, depth and atmosphere. It may not invent brand marks, contact details or decorative pseudo-branding.

## Public CTA law

`constitution/PUBLIC_CTA.md` is the only authority for public CTA. The business WhatsApp number `050-2517000`, WhatsApp wording, phone icons paired with the number, and `wa.me` are forbidden in public creative/captions unless the owner explicitly requests that exact public use in the current task.

Default public CTA: Instagram `@velvets_cloud` / message on Instagram + pickup in Sderot. A showcase may use no CTA at all.

## Required exact-final evidence

Before a public visual can PASS:

- `brand_asset_gate: PASS`
- `generated_brand_mark: NONE`
- `logo_usage: NONE | VERIFIED_ASSET`
- `logo_source_ref: NONE | <exact verified asset ref>`
- `logo_render_method: NONE | DETERMINISTIC_OVERLAY`
- `public_phone_absent: PASS`
- `public_cta_gate: PASS`

If `logo_usage=VERIFIED_ASSET`, `logo_source_ref` must identify the exact approved asset and `logo_render_method` must be `DETERMINISTIC_OVERLAY`. Any AI-invented logo/wordmark or public phone number is an automatic FAIL and repair before owner review.