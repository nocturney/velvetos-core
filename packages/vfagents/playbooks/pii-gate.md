# שער PII — לפני מודל חיצוני

מקור: `agents/21-pii-sanitization-agent`.  
חבילות: `vlicense`, `vfops`.  
אין סיסמאות, קודי אימות, או שמות לקוח מיותרים בגיט.

## מתי חובה

לפני הדבקה ל־GPT / Gemini / Treg / כל מודל מחוץ למשרד.

## מה מוחקים או מחליפים

| סוג | מחליפים ב |
|---|---|
| טלפון לקוח | `[טלפון]` |
| מייל לקוח | `[מייל]` |
| שם מלא | קוד עבודה / «לקוח» |
| כתובת מגורים | `[כתובת]` — איסוף נשאר «שדרות» |
| ת״ז / ח״פ | `[מזהה]` |
| אמצעי תשלום / חשבון / Invoice4U חי | `[תשלום]` |
| סיסמה / קוד / מפתח API | מוחקים. לא מחליפים — עוצרים |

מספר הסטודיו `050-2517000` בנהלים — נשאר. זה לא PII של לקוח.

## פלט

```
עבר שער: כן / לא
מה הוסר (סוגים, לא הערכים):
טקסט נקי להדבקה:
```

אם `לא` — לא מדביקים. אין «בערך בטוח».
