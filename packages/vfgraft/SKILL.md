# vfgraft

Office graph for Velvet Factory HQ. Pattern from [trailhq/Graft](https://github.com/trailhq/Graft). No npm install.

When the job is "how is HQ wired", "where do I start", blast radius, or a cold agent is about to grep the warehouse:

1. Read `packages/vfgraft/MAP.md`.
2. Open two or three nodes under `packages/vfgraft/graph/`.
3. Follow `[[wikilinks]]`. Do not reopen every pack.
4. Keep the desk laws. Do not send. Do not invent ₪.

Mention `@codebase-onboarding-engineer` only if the user asks for that warehouse slug. The graph is enough for a Sderot job.
