# packs

## Summary

Office OS lives in `packages/<name>/`. Origin trees plus HQ-native overlays (`SKILL.md`, `hq/`). Do not add a pack that duplicates an existing tool. Pattern packs (`vfe2b`, `vfagents`, `vfmcp`, `vfgraft`) map an outside list onto those folders — they are not a second runtime.

## Sources

- `packages/manifest.json`
- `packages/README.md`

## Links

- part_of [[desk]]
- depends_on [[laws]]
- uses [[tools]]

## Notes

Same-day catalog on every finished pack (`docs/BACKUP.md`). If Origin will not clone, still update the map. Eleven packs may stay `origin-slug-unknown` — do not invent `tmp-…`. Playbook: `docs/ORIGIN-SLUGS.md`.
