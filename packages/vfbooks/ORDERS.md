# הזמנות + Invoice4U לבריף 02

מושב: **תפעול**. לא אתר. לא פק חדש.

Invoice4U נשאר מערכת הסגירה אחרי סכום מראש צוות. HQ **לא** מחליף אותה ולא ממציא שורות.

## מקורות לבריף (דיסק בלבד)

| קובץ | מה | ריק = |
|---|---|---|
| `data/orders.json` | הזמנות פתוחות בלי שמות מיותרים | אין ספירה |
| `data/invoice4u-snapshot.json` | חוב / שולם שהבעלים הדביק | אין ספירה |
| `office/ledger/live/jobs.csv` | יומן עבודות (`vf_office.py jobs`) | אין ספירה |

הבריף 07:00 **לא** קורא `in:inbox`. תווית «חשבונות» נשארת ל־`vfbooks` בשרשור נקוב — לא למילוי אוטומטי של הבריף.

## כתיבה

אחרי סגירה אנושית בוואטסאפ + Invoice4U — להדביק שורה בסנאפשוט (סכום מאומת בלבד). בלי סכום: `X ₪` / ריק.  
אין API Invoice4U בגיט. אין מפתח.

חריץ 02 מדביק: `python3 scripts/vfops_loop.py brief` → שורת `ספר: …`.
