# vfmakers locks

נעילות מהרשימה של makerskills. לא מתקינים את הפלאגין. לא שולחים. לא ממציאים ₪.

## פלאגין / קונפיג אישי — דלג

`/plugin marketplace add coreyhaines31/makerskills`, `MAKERSKILLS_CONFIG`, `~/.config/makerskills/`, Typefully workspace IDs.

למה: Cursor כבר המשרד. קונפיג אישי מחוץ לגיט הוא מקור לסודות. הארכיונים אצלנו חיים ב־`packages/*/hq/`.

## לולאות אוטונומיות — דלג

`loopify`, `ScheduleWakeup`, cron על שליחה / גבייה / פרסום.

למה: תזמורת 06:15 כבר ב־`constitution/ORCHESTRA.md`. Grok מחליט. HQ לא רץ בלי אדם.

## כסף ביתי / בנק חי — דלג

`personal-cfo` (משק בית). Mercury / Plaid / Stripe / Ramp / Gusto מ־`company-cfo`.

למה: ספרי הסטודיו הם `vfbooks` + תווית ג׳ימייל «חשבונות» + Invoice4U. אין מחסן SQL. אין ₪ בלי מקור. אין `git add` לקבצי בנק.

## רשתות לא שלנו — דלג כפעולה חיה

X / LinkedIn / Typefully publish מ־`jab-hook`. דומיין `.com` מ־`domain`. דק Next.js מ־`slide-deck`.

למה: הערוץ הוא אינסטגרם `@velvets_cloud`. HQ סוקר ומשבץ. Grok שולח. CTA הוא וואטסאפ `050-2517000` / איסוף שדרות — לא «שלחו DM».

## מועצה מפורסמת / רעיון מוצר חדש — דלג או אחר כך

`maker-council` (Fried / Musk / …). `business-brainstorm` כפתיחת קו B2B.

למה: B2B נעול ב־`vfbiz` (לוגו, QR, מפיות) עד ראש צוות. אין המצאת אסטרטגיה בשם סלבס.

## מוח שני מחוץ ל־HQ — דלג

`second-brain` (כספת אישית). `company-brain` כ־Obsidian / Fathom / Gong / CRM sync.

למה: הקטלוג + `packages/*/hq/` הם הזיכרון. אין העלאת קבצי סטודיו לכלי שלישי. אין תיקיות רפואיות / משפטיות / אישיות מדרייב אלא אם המשתמש נקב בשם.

## כלי חיבור / סוכן קוד — דלג

`toolify` כמתקין MCP/API חדש. `skillify` כמפעל מיומנויות בלי בקשה.

למה: הכלים על השולחן כבר רשומים ב־`.cursor/vf-desk.json`. מיומנות חדשה רק אם ראש צוות מבקש.
