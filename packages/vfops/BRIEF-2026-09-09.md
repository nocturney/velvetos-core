# בריף · 9.9.2026 · עותק יום

Asia/Jerusalem. מקור: `python3 scripts/vfops_loop.py brief --write --date 2026-09-09`.  
JSON: `packages/vfops/hq/brief-2026-09-09.json`.

**Instagram MCP remote:** Cloud Run live (`instamcp` / `me-west1` / `velvet-instagram-mcp`) · `remote_access=ready` אחרי healthcheck · `@velvets_cloud` מאומת · bearer gate · DM כבוי · stdio = fallback מקומי. אין סודות בגיט.

Insights: אין ספירה בלי snapshot בעלים.  
אין ₪ מכירה מומצא. אין Publish בלי שערי אישור + liveVerified.
