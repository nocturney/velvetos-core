# vfmedia — נעילה

לא פק מושב שישי. קטלוג מדיה יחיד על כספת Drive שנעל Christian (8.9.2026).

1. קטלוג אחד: `catalog.json`. אין גיליון / JSON / תיבת INSTA מקבילים.
2. תפעול = בעל קליטה (נכנס → מקור).
3. העלאה ≠ אישור.
4. תיקיית «מאושר לפרסום» לבד ≠ הוכחת אישור גרסה.
5. לא ממציאים ₪ / Insights / מק״ט / סצנת רצפה.
6. אין שינוי הרשאות שיתוף ציבורי מ־HQ. אין מחיקת קבצים.
7. GrokBot משתמש ב־Drive MCP. Cursor מחזיק סכמה. שיבוץ נשאר `vfigos` + `PREFLIGHT`.
8. `productLink` רק ממקור ידוע; אחרת `null`.
