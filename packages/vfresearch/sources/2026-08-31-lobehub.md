# סקירת LobeHub · 2026-08-31

מושב: ייצור · Asia/Jerusalem  
רישום: `packages/vfresearch/LINKS.json`  
מקור: https://github.com/lobehub/lobehub

## מה נבדק

| id | סטטוס | הערה |
|---|---|---|
| lobehub-operator | הוטמע (דפוסים) | לא מוצר / לא Docker / לא runtime שני |

LobeHub = «Chief Agent Operator»: Next.js monolith, MCP Connectors, Agent Groups, cron schedule, white-box memory, 10,000+ plugins marketplace.

## מה הוטמע

| דפוס LobeHub | אצלנו | קובץ |
|---|---|---|
| ToolsEngine — עדיפות connector + failover | טבלת מושב → namespace → גיבוי | `packages/vfmcp/GAP.md` |
| Operator — hire / schedule / report | כרטיס משמרת + checkpoint | `vfe2b/crews/run.md` + `vfharness/state/` |
| White-box memory | checkpoint schema (לא היסטוריית צ'אט) | `vfharness/templates/checkpoint.schema.json` |
| Agent Groups — הקשר משותף | fan-out מבוקר (max 3) על vfcopy/vfcovers | `vfe2b/crews/run.md` |
| Schedule registry | ROUTINE + בריף 07:00 | `vfops/ROUTINE.md` |

## מה לא הוטמע (מכוון)

| LobeHub | למה |
|---|---|
| Self-host Docker / Vercel | runtime שני — `vfe2b/LOCK.md` |
| IM Gateway / Telegram | נעול (OpenClaw, גשרים) |
| 10,000 MCP marketplace | רעש; desk ממוקד ב־`vf-desk.json` |
| cron 7×24 בלי שער | ₪ / WhatsApp / IG → `decision_gate` + אדם |
| Continual learning | סיכון להמצאת ₪ / Insights |
| `@lobehub/ui` | אין אפליקציה — הקטלוג הוא המוצר |

## מה חדש לחקור

- אין חובה. אם LobeHub מפרסם Connector migration guide חדש — pass שבועי ישווה ל־`vfmcp/GAP.md`.

## בלוק 05

הוטמעו דפוסי Operator/Connector על פקים קיימים — לא פק `vflobe`.
