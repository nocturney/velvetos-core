{
  "_comment": "Team MCP / Cloud Agent HTTP example for remote Instagram MCP. Copy into Team Integrations or cloud env — NOT into this repo's .cursor/mcp.json with real secrets. Fill INSTAGRAM_MCP_REMOTE_URL only after Fly deploy + successful remote healthcheck. Bearer is VELVET_INSTAGRAM_MCP_BEARER_TOKEN (MCP gate), never the Meta Graph token.",
  "mcpServers": {
    "instagram": {
      "type": "http",
      "url": "${env:INSTAGRAM_MCP_REMOTE_URL}",
      "headers": {
        "Authorization": "Bearer ${env:VELVET_INSTAGRAM_MCP_BEARER_TOKEN}"
      }
    }
  },
  "_fallback_stdio_dev": {
    "instagram": {
      "command": "instagram-mcp",
      "env": {
        "INSTAGRAM_MCP_ACCESS_TOKEN": "${env:INSTAGRAM_MCP_ACCESS_TOKEN}",
        "INSTAGRAM_MCP_IG_USER_ID": "${env:INSTAGRAM_MCP_IG_USER_ID}"
      }
    }
  },
  "_notes": [
    "Primary for Cloud autonomy: HTTP Streamable at /mcp after packages/vfigos/REMOTE.md deploy",
    "Fallback/dev: stdio via mcp.desktop.example.json",
    "Do not enable INSTAGRAM_MCP_DM_ENABLED",
    "Do not commit filled copies"
  ]
}
