# Publish artifact handoff

Manifests only (no binaries on `main`). Stage on branch `publish-bridge`.

CLI: `python3 scripts/vf_publish_handoff.py`

See `PUBLISH-BRIDGE.md` § Artifact handoff / recovery.
