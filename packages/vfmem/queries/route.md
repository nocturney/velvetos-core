# route

מקור: Route nodes + `query_graph`.  
אצלנו: הצינור האחד. אין צינור שני.

## מתי

שאלה על שלב: פנייה / שיחה / הצעה / הדפסה / איסוף.

## הרצה

```bash
python3 scripts/vfmem.py route פנייה
python3 scripts/vfmem.py route הצעה
python3 scripts/vfmem.py route איסוף
```

אם זו לא מילת שלב — מחזיר את נתיב השולחן הכי קרוב.

## נעילה

איסוף בשדרות בלבד. אין משלוח ארצי מ־HQ. אדם + Invoice4U סוגרים.
