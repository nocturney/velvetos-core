# vfmem locks

נעילות מהריפו של DeusData. לא מתקינים את הבינארי מ־HQ. לא שולחים. לא ממציאים ₪.

## הבינארי / המתקין — דלג

`curl … \| bash`, `install.sh`, `codebase-memory-mcp` ב־`.cursor/mcp.json`, דמון תיאום, UI על `:9749`.

למה: המתקין כותב קבצי סוכן. MCP חי נשאר בהגדרות Cursor אחרי שכריסטיאן בוחר חשבון (`docs/MCP-FIT.md`). הריפו הזה נשאר מפה.

## Hybrid LSP / 162 שפות / גרף AST — דלג

למה: המשרד הוא פקים ו־JSON, לא מוצר קוד. כמה סקריפטי פייתון לא מצדיקים אינדקס ליבת לינוקס.

## אמבדינגים / nomic-embed-code — אחר כך

רק אם ראש צוות מבקש חיפוש סמנטי על הקוד. בינתיים חפיפת טוקנים על המפה מספיקה.

## מחסן Agency כ־dead code — דלג כמחיקה

245 סוכנים כבויים בכוונה. `vfmem.py dead` מדווח; לא מוחקים.

## שליחה / מחיר — נעילה קבועה

אין send / reply / forward. אין פוסט Instagram. אין ₪ או Insights מומצאים.
