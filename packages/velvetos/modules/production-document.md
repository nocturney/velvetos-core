# Production — document

Module id: `production-document`

## Provides
Versioned drafting of opinions/reports: intake materials → draft → human sign-off → deliver.

## Packs
\`vfprod\`, \`vlicense\`, \`vfsales\`

Always present in core. An instance enables it via `modulesEnabled` — this is not an on/off goal toggle in a shared tenant list.
