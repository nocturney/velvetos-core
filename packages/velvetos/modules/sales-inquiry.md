# Office — sales / inquiry

Module id: `sales-inquiry`

## Provides
Inquiry chain on existing packs. HQ sends Gmail via tools when a draft is ready. No invented ₪.

Always present in core. An instance enables it via `modulesEnabled` — this is not an on/off goal toggle in a shared tenant list.
