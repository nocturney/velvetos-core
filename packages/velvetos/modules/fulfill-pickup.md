# Fulfillment — pickup

Module id: `fulfill-pickup`

## Provides
Customer collects in person. \`nationalShipping: false\` unless lead seat opens it.
Maps VF «איסוף».

## Packs
\`vfprod\`, \`vfsales\`

Always present in core. An instance enables it via `modulesEnabled` — this is not an on/off goal toggle in a shared tenant list.
