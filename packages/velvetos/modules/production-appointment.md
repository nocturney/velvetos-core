# Production — appointment

Module id: `production-appointment`

## Provides
Treat \`fulfill\` as a verified appointment. Station readiness, not a slicer queue.

## Packs
\`vfprod\`, \`vfops\`

Always present in core. An instance enables it via `modulesEnabled` — this is not an on/off goal toggle in a shared tenant list.
