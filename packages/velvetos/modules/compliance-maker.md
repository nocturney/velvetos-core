# Compliance — maker / studio

Module id: `compliance-maker`

## Extra locks
Pickup-only. Public offering follows `packages/vfbiz/OFFERING.md`; customer type or quantity never creates a separate service pillar. Public commercial files only; no TikTok/ads without lead.

Always present in core. An instance enables it via `modulesEnabled` — this is not an on/off goal toggle in a shared tenant list.
