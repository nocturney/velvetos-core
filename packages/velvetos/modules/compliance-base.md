# Compliance — base

Module id: `compliance-base`

## Hard locks (every instance)
\`noInventedPrices\`, \`noInventedInsights\`, \`noAutoDm\`, \`noBoostWithoutLead\`, \`hqSendViaTools\`

Always present in core. An instance enables it via `modulesEnabled` — this is not an on/off goal toggle in a shared tenant list.
