# הדפסת ניסיון ראשונה — שעה אחת למק״ט אחד

מושב: **ייצור**. לא הדפסה מ־HQ. לא פק חדש.

מקור תזמורת 2.9.2026: מדף 5 כרטיסים (ChatGPT 30.8) + רישיון לפי דגם (לא לפי פלטפורמה).  
מטרה: למלא **משבצת אחת** ב־`SHELF.json` בלי להמציא שם, ₪, או כמות.

## לפני השעון

- בעלים / רצפה **נותנים** קישור לדגם. HQ לא ממציא MakerWorld / Printables.
- מותג ישראלי או דמות מוכרת → עצירה (`vlicense/GATE.md`).
- כרטיס ראשון על המדף: **בלי חומרה** (בלי מגנטים, מתגים, ברגים). חומרה = צהוב ב־`LAB.md`.

## 60 דקות — משבצת אחת

| דק׳ | מה | תוצאה על הדיסק |
|---|---|---|
| 0–10 | פותחים את דף הדגם. מעתיקים URL + מזהה. קוראים את שורת הרישיון **בדף**, לא את שם האתר. | `license` + `licenseProof` (צילום / ציטוט שורה) + `licenseChecked` (תאריך) |
| 10–15 | `vlicense/GATE.md`: ציבורי מסחרי / מנוי יוצר פעיל לדגם הזה / עצירה. הורדה ≠ מכירה. | `status=waiting-license` עד שיש הוכחה; אחרת `blocked` |
| 15–30 | סלייס על הרצפה: גרם + דקות + חומר. בלי נוסחת ₪. | `sliceGrams` + `sliceMinutes` + `material` |
| 30–40 | עשר השאלות ב־`LAB.md` → ירוק / צהוב / אדום. | `labColor` |
| 40–55 | אם ירוק: הדפסת ניסיון **אחת** על הרצפה (`vfprod/CHECKLIST.md`). HQ לא לוחץ הדפס. | הערה ברצפה; כשל → צהוב/אדום |
| 55–60 | ממלאים משבצת ב־`SHELF.json`. `status=ready` רק אחרי רישיון + סלייס + ניסיון שעבר + ספירה פיזית 0 או יותר. | `python3 scripts/vfsku.py shelf` |

אין באצ׳ בלי סכום מראש צוות. אין שם במשבצת בלי URL + רישיון.

## הוכחת רישיון (חובה לפני ready)

לא מספיק «זה ב־MakerWorld» / «זה ב־Printables» / «שילמתי על הקובץ».

רושמים במשבצת:

1. `sourceUrl` — דף הדגם.
2. `license` — הטקסט הקצר מהדף (למשל Standard Digital File / CC / Commercial membership).
3. `licenseProof` — איפה ההוכחה (צילום Drive / שורת FAQ / מנוי יוצר פעיל).
4. `licenseChecked` — תאריך בדיקה (`YYYY-MM-DD`).
5. מנוי MakerWorld Commercial License: בודקים שהמנוי **עדיין פעיל** ושהדגם נכלל («Get Commercial License» בדף). ביטול → גישה עד סוף מחזור החיוב (FAQ), ואז `waiting-license`.
6. התראת שינוי תנאים מ־MakerWorld → `licenseChecked` מחדש **לפני** באצ׳ הבא. אין באצ׳ על תנאים ישנים.

## אחרי ready

- כרטיס פרוזה: `CARDS.md` / `hq/PLAYBOOK.md`.
- ריל מדף: רק אחרי `ready` (`vfgrowth/CALENDAR.md`).
- מלאי על מדף האיסוף: רק ספירה פיזית בשדרות. חסר = «אין ספירה».
- בריף 03: `python3 scripts/vfsku.py brief`.

## לא כאן

המצאת שם דגם · נוסחת ₪ · באצ׳ · שכפול חנות · Printago/Shopify · חבילות STL המוניות · הדפסה מ־HQ.
