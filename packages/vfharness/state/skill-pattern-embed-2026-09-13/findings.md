# Findings

- `planning-with-files` and `last30days` were already materially embedded in `vfharness` / `vfresearch`.
- Existing Hebrew copy pipeline already covered many anti-AI patterns; the remaining `avoid-ai-writing` patterns are captured in `vfcopy/hq/AI-TELLS-GAP.md` for selective use.
- Existing Brand Guardian focuses social creative; internal product UI benefits from an explicit RTL/accessibility/design-system contract and a separate anti-slop audit.
- Security agents exist, but a deterministic committed agent-surface scan is useful as an AgentShield-style complement.
