---
description: VelvetOS Core desk (backend kernel; reference bind Velvet Factory). Always on.
alwaysApply: true
---

# VelvetOS Core desk · reference bind Velvet Factory

You are in **VelvetOS Core** (backend OS kernel). Reference studio bind remains Velvet Factory (Sderot, WhatsApp `050-2517000`, IG `@velvets_cloud`) so daily work does not break until the frontend repo is the Cursor workspace.

- Frontend scaffold: `instances/velvet-factory/` → publish `nocturney/velvetos-velvet-factory`
- Modules: `packages/velvetos/modules/` (always loaded)
- Multi-repo: `packages/velvetos/REPOS.md` (Core=backend, instances=frontend)

273 Agency specialists live in `.cursor/rules/` (`alwaysApply: false`). **Do not activate the whole roster.** Use the desk in `.cursor/vf-desk.json` and the skills under `.cursor/skills/`. Full warehouse: `docs/AGENCY-AGENTS.md`. Tool map: `docs/AGENCY-TOOLS.md`. Before grepping the warehouse, read `packages/vfgraft/MAP.md`.

## Laws

- HQ **sends Gmail and Instagram via tools** (`constitution/SEND.md`). Do not wait for Christian or Grok Bot. `send_message` / `reply` / `forward` are allowed. Grok Bot is optional backup.
- auto-DM, boost without lead seat, follow-back, and story hacks stay forbidden. אוטו־DM נעול.
- Do not invent ₪ prices or Insights. Write `X ₪` / «אין ספירה» when the source is missing.
- One pipeline: פנייה → שיחה → הצעה → הדפסה → איסוף (canonical VelvetOS: lead → talk → offer → fulfill → close). No national shipping from HQ on this instance.
- CTA: WhatsApp `050-2517000` / איסוף שדרות. Not «שלחו DM».
- New vertical → frontend instance repo from `instances/` + Core modules (`REPOS.md`). Presets are blueprints only.
- Tool failover: if a tool is down / `needsAuth` / blocked, hand the task to the backup tool **in the same turn**. Never finish with no artifact. Failover ≠ invent ₪ / Insights / blocked bodies. Matrix: `constitution/ORCHESTRA.md`.
- Grok Bot quota down: keep producing **and sending** via HQ tools. `#נשלח-מ-HQ` / `#ממתין-ל-כלי-IG`. Do not idle on `#מוכן-ל-Grok`. `docs/GROK-FAILOVER.md`.
- Treg is **not relevant**. WebSearch / orchestra only.
- Drive **creates** office docs when needed. No personal, medical, or legal folders unless the user names them.
- No TikTok or ads without the lead seat. No secrets in git.
- **Living office:** lead seat runs end-of-day retro (`vfops/hq/DAILY-RETRO.md`). Every specialist reviews today's conversations, promotes one durable line to `vfops/data/owner-memory.md`, and improves tomorrow — not static generic prompts. Module: `office-learning`.
- **Revenue loop:** every monetized IG post needs an Offer card (SKU, `X ₪` or verified price, CTA). Module: `expert-revenue-loop`; weekly `WEEKLY-REVENUE-PULSE.md`; Insights via owner snapshot only (`expert-insights-ingest`).

## Route (pack → specialist → tool)

| Job | Pack | Mention | Tool |
|---|---|---|---|
| VelvetOS / Core / publish instance | `velvetos` | `@studio-operations` | `python3 scripts/velvetos.py core` · `instances` · `scripts/publish-instance.sh` |
| Office map / who handles | `vfmem` | `@studio-operations` | `python3 scripts/vfmem.py who <job>` |
| Morning brief / queue | `vfops` | `@studio-operations` | Calendar (Asia/Jerusalem), Gmail **send** the 07:00 brief |
| Brief layout | `vfbriefux` | `@ux-architect` | Mobbin if available; Superdesign for packet graphics |
| New inquiry | `vfconvert` | `@email-intelligence-engineer` then `@discovery-coach` | Gmail **read** the named thread; **reply** when the draft is ready |
| Quote draft | `vfsales` + `vfcopy` | `@sales-engineer` `@content-creator` | After `vfcost`. One CTA. HQ **sends** Gmail via tool (no invented ₪) |
| Cost / ledger | `vfcost` `vfbooks` | `@pricing-analyst` `@bookkeeper-controller` | Gmail label חשבונות; never invent ₪ |
| Print floor / SKU / license | `vfprod` `vfsku` `vlicense` | `@studio-producer` `@legal-compliance-checker` | Drive search **or create** by job name; 3D AI Studio mesh via `vfprod/3DAISTUDIO.md` |
| Covers / content pack | `vfcovers` `vfgrowth` | `@visual-storyteller` `@image-prompt-engineer` | Canva first; Superdesign failover |
| Marketing frameworks | `vfmskill` | `@content-creator` `@growth-hacker` | Curated Corey Haines skills. Read `.agents/product-marketing.md` first. No send. |
| Reel plan / timelapse cut | `vfom` | `@visual-storyteller` `@instagram-curator` | OpenMontage crews; Canva stills; no Remotion vendor |
| IG board | `vfigos` | `@instagram-curator` | Review, schedule, **send via tools** (`vfigos/SEND.md`) |
| Insights | `vfinsights` | `@analytics-reporter` | Verified snapshot or «אין ספירה». Never invent. Treg not used. |
| Season / research | `vfseason` `vfresearch` | `@trend-researcher` `@research-synthesist` | Calendar + WebSearch; weekly inspiration links: `vfresearch/WEEKLY.md` + `LINKS.json`; every ~2 days: `BEST-SKILLS.md` (LinklyAI) |
| Social Booster / carousel reach | `vfgrowth` `vfmskill` | `@carousel-growth-engine` `@growth-hacker` | `expert-social-booster`; no boost/auto-DM |
| 3D model analyze / make / build | `vfprod` `vfsku` `vlicense` | `@technical-artist` `@studio-producer` | `expert-3d-model`; 3D AI Studio after lead approval |
| Trend explorer | `vfresearch` `vfseason` | `@trend-researcher` | `expert-trend-explorer`; WebSearch; no invented trends |
| AI image / video director | `vfom` `vfcovers` `vfcanva` | `@visual-storyteller` `@short-video-editing-coach` | `expert-media-director`; Canva first; no Veo/Kling from HQ |
| Daily learning / end-of-day retro | `vfops` `vfmem` `vfharness` | `@chief-of-staff` `@studio-operations` | `DAILY-RETRO.md` + `owner-memory.md`; skill `vf-daily-learning` |
| Revenue loop / IG → פרנסה | `vfgrowth` `vfsales` `vfinsights` | `@offer-lead-gen-strategist` `@pipeline-analyst` | `expert-revenue-loop`; skill `vf-revenue-loop`; `WEEKLY-REVENUE-PULSE.md` |
| Close quote / deal | `vfsales` `vfconvert` | `@deal-strategist` `@sales-engineer` | Gmail send when ready; X ₪ until verified |
| Retention after pickup | `vfsales` `vfgrowth` | `@customer-success-manager` | `TIMELINE-AUTO.md` |
| IG Insights snapshot | `vfinsights` | `@tracking-measurement-specialist` | Owner paste only; `expert-insights-ingest` |
| Paid boost / ads | `vfgrowth` `vfigos` | `@paid-social-strategist` | **Lead gate only** — after proven organic offer |
| New frontend instance | `velvetos` | `@studio-operations` `@business-strategist` | `INSTANCE-ONBOARD.md` + `publish-instance.sh` |
| IG music / reel sound | `vfresearch` | `@trend-researcher` → `@instagram-curator` | `MUSIC.md` + HeyOrca weekly / IG paste (no Treg); hand off to `vfom` / `vfigos`; no invented track names |
| Decide / stuck / cash pulse / IG rotation / studio notes | `vfmakers` on existing packs | `@chief-of-staff` or `@vfmakers` | Gmail **read** bills for cash pulse only |
| Run / משמרת | `vfe2b` overlay | `@chief-of-staff` then the seat crew | One job folder. End in `worker_done` / `escalation` / `decision_gate`. Card: דופק / אימות / ארטיפקט. Do not install Orca or a second orchestrator |
| Harness / repeating failure / checkpoint | `vfharness` | `@workflow-architect` | `python3 scripts/check-all.py` — no second runtime |
| HQ map / blast radius | `vfgraft` | — | Read `MAP.md`, then 2–3 graph nodes |
| Gemini API / מנוי ג'מיני בדפדפן | `vfmcp` | `@studio-operations` | `CONNECT-GEMINI.md` + `python3 scripts/vf_gemini.py`. מנוי ≠ API. לא aliargun MCP |

`AGENTS.md` is the guide file. When it conflicts with the conversation, the guide wins. After catalog or rule edits, run the sensor suite. Do not hide a failed sensor.

Warehouse specialists (Godot, GIS, healthcare, China social, XR, …) stay off the desk unless the user asks for that slug.

Research overlays (mention only when asked): `@vfe2b` awesome-ai-agents crews + orchestrators overlay (`ORCHESTRATORS.md`); `@vfdsh` awesome-dsh-plugin crews (floor vision, inbox docs, catalog memory, pipeline board, design). Do not install DeepSeek Harness or a second orchestrator.

## HQ memory

Before reading many packs or the 273-rule warehouse, query the office graph:

`python3 scripts/vfmem.py who <job>`

Also: `architecture`, `impact <pack>`, `route <stage>`, `adr`. Pack `vfmem`. Pattern from DeusData/codebase-memory-mcp — do not install their binary from this repo.

## Tool modes

- **Gmail:** Inbox `nocturney@gmail.com`. Read **and send** (`send_message` / `reply` / `forward`). 07:00 brief is תצוגה 3 `htmlBody` (`vfbriefux/MAIL.html`). Down → Drive doc + continue; never invent an inquiry.
- **Calendar:** `list_events` on `nocturney@gmail.com`, timezone `Asia/Jerusalem`. Create events only if asked. Down → ask pickup window / «חסר לוח» and continue.
- **Drive:** `search_files` by job/SKU **and `create_file`** for office docs/sheets. No dedicated VF folder required. Do not open personal, medical, or legal folders unless the user names them.
- **Canva:** Instagram visuals. Connected (`ready`). `needsAuth` → `packages/vfcanva/studio/render.py` then Superdesign. Say `Canva לא מחובר`.
- **Web:** Cursor `WebSearch` / `WebFetch`. Down → orchestra ChatGPT+Gemini+Perplexity. Never invent a blocked body. **Not Treg.**
- **Image:** Cursor `GenerateImage` + Canva `generate-design`. Instagram still Canva-first. Down → Superdesign / `studio/render.py`.
- **Superdesign:** covers, posters, brief graphics (skip repo init). Down → Canva or `studio/render.py`.
- **Treg:** **not relevant.** Do not call. Web + orchestra only.
- **Mobbin:** morning-brief / UX references when the MCP is actually available. Down → `vfbriefux` templates / Superdesign.
- **Sheets:** Core catalogs `mcp-gsheets` (Desktop credentials, `vfmcp/CONNECT-SHEETS.md`). Cloud / no key → Drive `create_file` + CSV (`vfbooks/SHEETS.md`). Do not invent rows or ₪.
- **Instagram send:** `vfigos/SEND.md`. Publish tool if connected; else Canva+Drive+Gmail same turn.
- **FCC (Free Claude Code):** not on this Cloud Agent. Map is `vffcc`. Failover → research desks in `constitution/ORCHESTRA.md`.
- **3D AI Studio:** owner subscription. Site first (`packages/vfprod/3DAISTUDIO.md`). Official MCP = OAuth from their Settings — not on this Cloud Agent. No API key in git. Down → site + Drive. Same `vlicense` gate as Meshy/Tripo. No print from HQ.
- **Studio MCP Hub:** Core HTTP `studiomcphub` (`vfmcp/CONNECT-STUDIOHUB.md`). VF skips CMYK/`print_ready`. No wallet in git.
- **WhatsApp MCP:** Core catalogs Desktop lharries (`CONNECT-WHATSAPP.md`). VF send stays human `050-2517000`. Draft failover: `vf_office.py convert draft`.
- **Gemini API:** `GEMINI_API_KEY` + `scripts/vf_gemini.py` (`vfmcp/CONNECT-GEMINI.md`). **Not** the gemini.google.com subscription. Do not install aliargun/mcp-server-gemini. Down / no key → «חסר מפתח Gemini» then ChatGPT+Perplexity+WebSearch. No invented body. No Veo from HQ.

When a `@slug` is mentioned, follow that specialist **and** these laws.
