---
description: Velvet Factory desk for e2b-dev/awesome-ai-agents patterns. Activate with @vfe2b.
alwaysApply: false
---

# vfe2b — Awesome-AI-agents desk

You are applying patterns from https://github.com/e2b-dev/awesome-ai-agents onto Velvet Factory HQ.

Read `packages/vfe2b/EMBED.md` and run one crew from `packages/vfe2b/crews/`:

- morning brief → `morning-brief.md`
- research → `research.md`
- inquiry → `inquiry.md`
- content → `content.md`
- books / numbers → `books-data.md`
- run / משמרת (Orca overlay) → `run.md` — one job, one existing crew, one of `worker_done` / `escalation` / `decision_gate`. Do not install Orca.

## Locks (do not break)

- HQ sends Gmail and Instagram via tools (`constitution/SEND.md`). Printers stay on the floor. No auto-DM. No boost.
- Do not invent prices (₪) or Insights metrics.
- Do not install AutoGPT / BabyAGI / a second coding agent / Orca ADE / a 24-7 chatbot.
- Human on WhatsApp for material / qty / when / finish. Sale shekels after head of desk.
- Missing source or number → write **חסר** or **אין במקור**. Do not fill the gap.

## Packs you may write notes into

Existing VF packs only (`vfigos`, `vfcost`, `vfconvert`, `vfgrowth`, `vfprod`, `vfsales`, `vfops`, `vfcovers`, `vfinsights`, `vfbooks`, `vfresearch`, `vfbiz`, `vfcopy`, `vlicense`, `vfseason`, `vfsku`, `vfbriefux`, `vfe2b`, `vfmakers`, `vfharness`). Do not create a new product pack unless asked.

Cursor is already the office. Do not vendor CrewAI, MemGPT, Julius, or Zapier.
